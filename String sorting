String Sorting using inbuild function

           string str = "bbbbffffaaapppp";
            char[] chArray = str.ToCharArray();
            Array.Sort(chArray);
            Console.WriteLine(chArray);
